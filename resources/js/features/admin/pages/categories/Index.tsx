import { Link, router } from "@inertiajs/react";
import AdminLayout from "@/layouts/AdminLayout";
import Button from "@/components/Button/Button";

type Category = { id: number; name: string; slug: string; image?: string | null; productsCount?: number; isActive: boolean };
export default function Index({ categories }: { categories: Category[] }) { return <AdminLayout><div className="mb-6 flex items-center justify-between"><div><p className="text-sm uppercase tracking-widest text-stone-500">Admin</p><h1 className="text-3xl font-bold">Categories</h1></div><Link href={route("admin.categories.create")}><Button>Add category</Button></Link></div><div className="overflow-x-auto rounded-2xl bg-white shadow-sm"><table className="w-full text-left text-sm"><thead className="border-b border-stone-200"><tr><th className="p-4">Category</th><th className="p-4">Slug</th><th className="p-4">Products</th><th className="p-4">Status</th><th className="p-4" /></tr></thead><tbody>{categories.map((category) => <tr key={category.id} className="border-b border-stone-100"><td className="p-4 font-medium">{category.name}</td><td className="p-4">{category.slug}</td><td className="p-4">{category.productsCount ?? 0}</td><td className="p-4">{category.isActive ? "Active" : "Inactive"}</td><td className="p-4 text-right"><Link href={route("admin.categories.edit", category.id)} className="mr-4 font-medium text-amber-700">Edit</Link><button type="button" className="font-medium text-stone-700" onClick={() => router.patch(route("admin.categories.status", category.id), { is_active: !category.isActive })}>{category.isActive ? "Deactivate" : "Restore"}</button></td></tr>)}</tbody></table></div></AdminLayout>; }
