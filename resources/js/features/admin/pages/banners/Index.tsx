import { Link, router } from "@inertiajs/react";
import AdminLayout from "@/layouts/AdminLayout";
import Button from "@/components/Button/Button";
type Banner = { id: number; title: string; sortOrder: number; isActive: boolean };
export default function Index({ banners }: { banners: Banner[] }) { return <AdminLayout><div className="mb-6 flex justify-between"><h1 className="text-3xl font-bold">Banners</h1><Link href={route('admin.banners.create')}><Button>Add banner</Button></Link></div><div className="overflow-x-auto rounded-2xl bg-white shadow-sm"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-4">Title</th><th className="p-4">Order</th><th className="p-4">Status</th><th className="p-4" /></tr></thead><tbody>{banners.map(banner => <tr key={banner.id} className="border-b"><td className="p-4 font-medium">{banner.title}</td><td className="p-4">{banner.sortOrder}</td><td className="p-4">{banner.isActive ? 'Active' : 'Inactive'}</td><td className="p-4 text-right"><Link className="mr-4 text-amber-700" href={route('admin.banners.edit', banner.id)}>Edit</Link><button className="mr-4" onClick={() => router.patch(route('admin.banners.status', banner.id), { is_active: !banner.isActive })}>{banner.isActive ? 'Deactivate' : 'Restore'}</button><button className="text-red-700" onClick={() => router.delete(route('admin.banners.destroy', banner.id))}>Delete</button></td></tr>)}</tbody></table></div></AdminLayout>; }
