import { Link, router } from "@inertiajs/react";
import AdminLayout from "@/layouts/AdminLayout";
import Button from "@/components/Button/Button";
type Faq = { id: number; question: string; sortOrder: number; isActive: boolean };
export default function Index({ faqs }: { faqs: Faq[] }) { return <AdminLayout><div className="mb-6 flex justify-between"><h1 className="text-3xl font-bold">FAQs</h1><Link href={route('admin.faqs.create')}><Button>Add FAQ</Button></Link></div><div className="overflow-x-auto rounded-2xl bg-white shadow-sm"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-4">Question</th><th className="p-4">Order</th><th className="p-4">Status</th><th className="p-4" /></tr></thead><tbody>{faqs.map(faq => <tr key={faq.id} className="border-b"><td className="p-4 font-medium">{faq.question}</td><td className="p-4">{faq.sortOrder}</td><td className="p-4">{faq.isActive ? 'Active' : 'Inactive'}</td><td className="p-4 text-right"><Link className="mr-4 text-amber-700" href={route('admin.faqs.edit', faq.id)}>Edit</Link><button className="mr-4" onClick={() => router.patch(route('admin.faqs.status', faq.id), { is_active: !faq.isActive })}>{faq.isActive ? 'Deactivate' : 'Restore'}</button><button className="text-red-700" onClick={() => router.delete(route('admin.faqs.destroy', faq.id))}>Delete</button></td></tr>)}</tbody></table></div></AdminLayout>; }
